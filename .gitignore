# Binaries (root only, not skills/claudemem/)
/claudemem
/claudemem-*
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
/test-data/
/tmp/

# OpenSpec archives (large over time)
openspec/changes/archive/

# Worktrees
.worktrees/
# runtime artifacts (created when running claudemem from the repo dir)
claudemem.db
memory.db
index.db
*.db-wal
*.db-shm
.claude/dev-progress/
